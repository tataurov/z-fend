@import "../scss/mixins";

body { background: #fff url(<%= asset_path 'dots.png'%>); color: #444; font-size: 14px; line-height: 1; height: 100%; }

/* main structure */
.base-width { max-width: 1200px; min-width: 990px; margin: 0 auto; }
.page-body { min-height: 100%; -webkit-box-sizing: border-box; -moz-box-sizing: border-box; box-sizing: border-box; padding-bottom: 40px; }
.page { padding: 10px; }
.footer-wrapper { height: 40px; margin-top: -40px; position: relative; @include boxShadow(0 0 5px rgba(0, 0, 0, .3)); }
.footer { padding: 10px 0; }

/* links */
a {
	&:link,
	&:active,
	&:visited,
	&:hover { color: #fff; }
}

h1 { margin-bottom: 15px; font-size: 200%; border-bottom: 2px solid #fff; }
h2 { margin-bottom: 15px; font-size: 150%; }

/* backgrounds */
.bg-dark { background: #3c3d41; }
.bg-main { background: #8da79a; }
.bg-light { background: #fdfdfd; color: #323232; }

/* center images */
.aci-wrap {
    display: table; table-layout: fixed;
    .aci-cell {
        display: table-cell; vertical-align: middle;
        .aci.img { display: block; margin: 0 auto; }
    }
}

/* animated objects */

.animated { @include transition(all .3s linear); }
